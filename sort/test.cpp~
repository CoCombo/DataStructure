#include <iostream>
#include "sort.h"
int main()
{
    std::vector<int> unSort = {0,3,2,1,6,7,5,13,10,12,11,4,8,9};
    //bubbleSort(unSort, unSort.size());
    //heapSort  (unsort, unsort.size());
    //InsertionSort(unSort, 0, unSort.size());
    quickSort(unSort, unSort.size());
    return 0;
}
